server:
  port: 8090
  
spring:
  application:
    name: apigateway
        
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8082/eureka
      
  instance:
    prefer-ip-address: true